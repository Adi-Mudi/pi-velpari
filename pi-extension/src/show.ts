import type { Stage } from "./constants.js";

/**
 * Print the published artifact for a stage. Phase A stub — full implementation
 * ships in Phase E (7 read paths).
 */
export function showStage(stage: Stage, cwd: string = process.cwd()): string {
	void cwd;
	void stage;
	return `[Phase A stub] Show not implemented yet for stage: ${stage}.`;
}
